package Api;

public class    SPI
{
    public Class getImplementation(Class interfaceToFind)
    {
        /*
        Iterator<IAnimal> it = ServiceLoader.load(interfaceToFind).iterator();

        String implementation = null;
        Class classToReturn = null;

        if(it.hasNext())
        {
            implementation = it.next().toString();
        }

        if(implementation != null)
        {

        }

        return classToReturn;
        */
    }
}
